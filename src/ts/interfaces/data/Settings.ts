export interface SettingsCounter {
  id: string;
  enterprise_id: string;
  allow_add_user: number;
  allow_edit_user: number;
  allow_delete_user: number;
  allow_edit_movement: number;
  allow_delete_movement: number;
}
